# build output
dist/
.astro/

# wrangler / cloudflare
.wrangler/
.dev.vars

# dependencies
node_modules/

# logs
npm-debug.log*
pnpm-debug.log*

# environment
.env
.env.*
!.env.example

# editors
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# dictionary build intermediates (compiled mnemonic-index.json and
# search-words.json ARE committed; words.json is regenerable and too big)
/data/dictionaries/_intermediate/
/data/dictionaries/compiled/words.json
design-candidates/
/tmp/

# OG-card output. Regenerated by `pnpm og:build`; chained into `pnpm build`
# so deploys always ship fresh cards. Local dev works without them (pages
# fall back to /og/default.png which 404s harmlessly until next build).
/public/og/
